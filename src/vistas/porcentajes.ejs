<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Porcentajes</title>
    <link rel="stylesheet" href="/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand">Porcentajes</a>
        </div>
    </nav>
    <div class="container">  
        <br>   
        <div>
            <a href="/porcentajes/nuevo" class="btn btn-primary">Nuevo</a>
            <a href="/" class="btn btn-secondary">Regresar</a>
        </div>
        <br>
        <div>
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Descripcion</th>
                        <th scope="col">Porcentaje</th>
                        <th colspan="2" scope="col">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <%
                    for(var i=0; i<data.length; i++)
                    {
                    %>
                    <tr>
                        <th scope="row"><%=data[i].id%></th>
                        <td><%=data[i].descripcion%></td>
                        <td><%=data[i].porcentaje%></td>
                        <td><a href="/porcentajes/editar/<%=data[i].id%>" class="btn btn-warning">Editar</a></td>
                        <td><a href="/porcentajes/eliminar/<%=data[i].id%>" class="btn btn-danger">Eliminar</a></td>
                    </tr>
                    <%}%>
                </tbody>
            </table>
        </div>
    </div>
    <script src="/bootstrap.bundle.min.js"></script>
</body>
</html>